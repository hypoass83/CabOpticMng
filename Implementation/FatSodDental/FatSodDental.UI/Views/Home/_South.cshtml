@using FatSod.Ressources;
@using Ext.Net.MVC;
@{
    var X = Html.X();    
}

@(
    X.Panel()
        .Region(Region.South)
        .BodyStyle("background-color:transparent;")
        .Border(false)

        .Items(
            X.Panel()
                .Layout(LayoutType.HBox)
                .PaddingSpec("0 5px 5px 5px")
                .Defaults(X.Parameter().Name("margins").Value("0 5 0 0").Mode(ParameterMode.Value))
                .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Middle, Pack = BoxPack.Center })
                .BodyStyle("background-color:transparent;")
                .Border(false)
                //.Items(
                //    X.Label()
                //        .Text("Resources.Licence_product_info +  ")
                //            ,
                //        X.HyperLink()
                //            .NavigateUrl("http://www.betterplanning.net")
                //            .Text("http://www.betterplanning.net")
                //            .Target("_blank")
                //)
        )
)